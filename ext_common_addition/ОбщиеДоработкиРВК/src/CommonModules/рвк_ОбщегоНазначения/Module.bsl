#Область ПрограммныйИнтерфейс

// Значение свойства.
// 
// Параметры:
//  Объект - ЛюбаяСсылка
//  ИмяСвойства - Строка
//  ЗначениеПоУмолчанию - Произвольный
// 
// Возвращаемое значение:
//  Произвольный - Значение свойства
Функция ЗначениеСвойства(Объект, ИмяСвойства, ЗначениеПоУмолчанию) Экспорт
	Результат = ЗначениеПоУмолчанию;
	ЭтоНашаОрганизация = УправлениеСвойствами.ЗначениеСвойства(Объект, ИмяСвойства);
	Если ЭтоНашаОрганизация <> Неопределено Тогда
		Результат = ЭтоНашаОрганизация;
	КонецЕсли;
	Возврат Результат;
КонецФункции

Функция СсылкуИзНавигационной(Знач НС) Экспорт
    
    НС = СтрЗаменить(НС, "<a href = """, "");
    НС = СтрЗаменить(НС, """>Основное</a>", "");
    
    ПерваяТочка = Найти(НС, "e1cib/data/");
    ВтораяТочка = Найти(НС, "?ref=");
    
    ПредставлениеТипа = Сред(НС, ПерваяТочка + 11, ВтораяТочка - ПерваяТочка - 11);
    ШаблонЗначения = ЗначениеВСтрокуВнутр(ПредопределенноеЗначение(ПредставлениеТипа + ".ПустаяСсылка"));
    ЗначениеСсылки = СтрЗаменить(ШаблонЗначения, "00000000000000000000000000000000", Сред(НС, ВтораяТочка + 5));
    
    Возврат ЗначениеИзСтрокиВнутр(ЗначениеСсылки);
   	 
КонецФункции

#КонецОбласти